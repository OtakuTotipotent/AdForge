import { SubscriptionPlan, UserRole } from "@/features/user";

export const DEFAULT_USER_CREDITS = 20;

export const DEFAULT_USER_ROLE = UserRole.USER;

export const DEFAULT_SUBSCRIPTION = SubscriptionPlan.FREE;
